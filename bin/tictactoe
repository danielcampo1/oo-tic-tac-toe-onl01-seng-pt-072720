#!/usr/bin/env ruby
require 'pry'
require_relative '../lib/tic_tac_toe.rb'

puts " Welcome to TicTacToe"
board = Array.new(9, " ")

turn(board)
turn(board)
turn(board)
turn(board)
turn(board)
turn(board)
turn(board)
turn(board)
turn(board)
